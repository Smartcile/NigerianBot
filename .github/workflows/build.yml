name: Build & push images

# Builds the service Docker images on GitHub's runners and pushes them to the
# GitHub Container Registry (GHCR). Portainer then just pulls the prebuilt
# images, so the server never compiles Rust.

on:
  push:
    branches: [main]
    paths:
      - "bot/**"
      - "api/**"
      - "common/**"
      - "scheduler/**"
      - "worker/**"
      - "migrations/**"
      - "Cargo.toml"
      - "Cargo.lock"
      - "Dockerfile.*"
      - ".github/workflows/build.yml"
  workflow_dispatch: {}

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      matrix:
        include:
          - dockerfile: Dockerfile.bot
            image: nigerianbot-bot
          - dockerfile: Dockerfile.api
            image: nigerianbot-api
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # GHCR requires a lowercase image path.
      - name: Compute image name
        id: img
        run: echo "name=ghcr.io/${GITHUB_REPOSITORY_OWNER,,}/${{ matrix.image }}" >> "$GITHUB_OUTPUT"

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ${{ matrix.dockerfile }}
          push: true
          platforms: linux/amd64
          tags: |
            ${{ steps.img.outputs.name }}:latest
            ${{ steps.img.outputs.name }}:${{ github.sha }}
          # Registry-based layer cache on GHCR (reliable, unlike type=gha which
          # 504'd). Combined with cargo-chef in the Dockerfiles, unchanged
          # dependencies are reused so only changed code recompiles.
          cache-from: type=registry,ref=${{ steps.img.outputs.name }}:buildcache
          cache-to: type=registry,ref=${{ steps.img.outputs.name }}:buildcache,mode=max
