# Keep the Docker build context small and never ship secrets.
target/
**/target/
.git/
.github/
.env
.env.*
!.env.example
# Exclude dashboard build artifacts, but keep the source (the API embeds
# dashboard/index.html at build time via include_str!).
dashboard/node_modules/
dashboard/build/
dashboard/dist/
*.log
